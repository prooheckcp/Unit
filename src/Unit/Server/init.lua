local shared = script.Parent.Shared

local MonoBehaviour = require(shared.MonoBehaviour)

local Unit = {}
Unit.MonoBehaviour = MonoBehaviour

--[[
    Starts Unit
]]
function Unit:Start()
    
end

return Unit